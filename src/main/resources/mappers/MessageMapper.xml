<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.green.tour.dao.MessageDAO">
	<insert id="writeMessage">
	insert into message(title, receiver_id, sender_id, content, del, send_date, read_check)
			    values(#{message.title},#{message.receiver_id},#{message.sender_id},#{message.content},'N',now(), 0)
	</insert>
	
	<!--받은 메시지  --> 
	<select id="getMessage" resultType="kr.green.tour.vo.MessageVO">
		select * from message where receiver_id = #{message.receiver_id} order by message_id asc
	</select>
	
</mapper>